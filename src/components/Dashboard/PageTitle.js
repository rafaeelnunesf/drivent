import styled from 'styled-components';

const PageTitle = styled.h1`
  font-family: 'Arial';
  font-style: normal;
  font-weight: 400;
  font-size: 34px;
  line-height: 40px;
  color: #000000;
`;

export default PageTitle;
